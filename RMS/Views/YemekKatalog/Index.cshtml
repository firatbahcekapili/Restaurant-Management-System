@model IEnumerable<RMS.Models.YemekKatalog>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KatalogAd)
        </th>
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            
            @Html.DisplayFor(modelItem => item.KatalogAd)
        </td>
     
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KatalogId }) |
            @Html.ActionLink("Details", "Details", new { id=item.KatalogId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KatalogId })
        </td>
    </tr>
}

</table>
